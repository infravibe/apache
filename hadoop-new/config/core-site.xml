<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>
    <!-- Default file system -->
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://namenode:9820</value>
        <description>The default file system URI</description>
    </property>

    <!-- Temporary directory -->
    <property>
        <name>hadoop.tmp.dir</name>
        <value>/tmp/hadoop-${user.name}</value>
        <description>Temporary directory for Hadoop</description>
    </property>

    <!-- Proxy users for security -->
    <property>
        <name>hadoop.proxyuser.root.hosts</name>
        <value>*</value>
        <description>Hosts from which root can impersonate other users</description>
    </property>

    <property>
        <name>hadoop.proxyuser.root.groups</name>
        <value>*</value>
        <description>Groups that root can impersonate</description>
    </property>

    <property>
        <name>hadoop.proxyuser.hive.hosts</name>
        <value>*</value>
        <description>Hosts from which Hive can impersonate other users</description>
    </property>

    <property>
        <name>hadoop.proxyuser.hive.groups</name>
        <value>*</value>
        <description>Groups that Hive can impersonate</description>
    </property>

    <!-- HTTP static user -->
    <property>
        <name>hadoop.http.staticuser.user</name>
        <value>root</value>
        <description>Default user for HTTP static content</description>
    </property>

    <!-- Security settings -->
    <property>
        <name>hadoop.security.authentication</name>
        <value>simple</value>
        <description>Authentication mechanism (simple/kerberos)</description>
    </property>

    <property>
        <name>hadoop.security.authorization</name>
        <value>false</value>
        <description>Enable authorization</description>
    </property>

    <!-- IO settings -->
    <property>
        <name>io.file.buffer.size</name>
        <value>131072</value>
        <description>Buffer size for file I/O operations</description>
    </property>

    <!-- Compression settings -->
    <property>
        <name>io.compression.codecs</name>
        <value>org.apache.hadoop.io.compress.DefaultCodec,org.apache.hadoop.io.compress.GzipCodec,org.apache.hadoop.io.compress.BZip2Codec,org.apache.hadoop.io.compress.Lz4Codec,org.apache.hadoop.io.compress.SnappyCodec</value>
        <description>Compression codecs</description>
    </property>

    <!-- Native library settings -->
    <property>
        <name>hadoop.native.lib</name>
        <value>true</value>
        <description>Use native Hadoop libraries</description>
    </property>

    <!-- RPC settings -->
    <property>
        <name>ipc.client.connection.maxidletime</name>
        <value>10000</value>
        <description>Max idle time for RPC connections</description>
    </property>

    <property>
        <name>ipc.server.tcpnodelay</name>
        <value>true</value>
        <description>Enable TCP_NODELAY for RPC server</description>
    </property>
</configuration>